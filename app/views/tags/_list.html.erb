				<div class="card">
					<div class="card-block">
	<h4 class="card-title">Теги</h4>
<ul>
		<% @tags.each do |t| %>
		    <li>
			    <%= link_to t.name, posts_url(tag: t.name), class: "tag-link" %>
			    <span class="badge badge-dark"><%= t.posts_count %></span>
			    <span class="badge badge-dark">+</span>
			  </li>
		<% end %>
</ul>
</div>
</div>