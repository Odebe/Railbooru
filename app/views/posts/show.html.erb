
<div class="row">
	<div class="col-lg-2  col-md-2 col-sm-12">
		<div id="left_bar">		
				<%= render "posts/actions_buttons.html.erb" %>
        <%= render "tags/list.html.erb" %>
        <%= render "posts/info.html.erb" %>
    </div>
	</div>
	<div class="col-lg-10 col-md-10 col-sm-12">
		<div class="row">
				<div id="post-image">
					<%= link_to "Image was scaled. Link to full image (пока нет)","#"  if @post.has_sample?%><br>
					<%= post_show_image(@post) {|image| image_tag image, class: "img-fluid"} %>	
				</div>
			
		</div>

	<%= render @post.comments %>
	<%= render 'comments/new_comment.html.erb'%>

	</div>
</div>